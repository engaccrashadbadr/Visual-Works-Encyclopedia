import { useMemo, useState } from "react";
import { Link } from "wouter";
import { ArrowLeft, Search, Star } from "lucide-react";
import { trpc } from "@/lib/trpc";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
export default function ComparePage() { const [ids, setIds] = useState<number[]>([1, 2]); const [raw, setRaw] = useState("1,2"); const input = useMemo(() => ({ ids: ids.slice(0, 2) }), [ids]); const { data } = trpc.catalog.compare.useQuery(input); const run = () => setIds(raw.split(",").map(Number).filter(Boolean).slice(0, 2)); return <div className="min-h-screen bg-background"><header className="border-b border-border/60"><div className="container flex h-20 items-center justify-between"><Link href="/" className="font-display text-xl font-bold">Visual Works <span className="text-primary">/</span> Compare</Link><Link href="/search" className="text-sm text-muted-foreground">Back to catalog</Link></div></header><main className="container py-12"><div className="eyebrow">SIDE BY SIDE / مقارنة</div><h1 className="section-title">Compare profiles</h1><p className="mt-4 text-muted-foreground">Choose two character or unit IDs to view their catalog fields side by side.</p><div className="mt-8 flex max-w-xl gap-2"><Input value={raw} onChange={e => setRaw(e.target.value)} placeholder="Entity IDs, e.g. 1,2"/><Button onClick={run}><Search size={16}/> Compare</Button></div><div className="mt-10 grid gap-5 md:grid-cols-2">{data?.map(entity => <Card key={entity.id} className="border-border/60"><CardContent className="p-6"><div className="flex gap-4"><img src={entity.imageUrl || "https://images.unsplash.com/photo-1578632767115-351597cf2477?auto=format&fit=crop&w=300&q=80"} className="h-24 w-20 rounded-xl object-cover" alt=""/><div><div className="text-xs uppercase tracking-wider text-primary">{entity.kind}</div><h2 className="font-display text-2xl font-bold">{entity.name}</h2><p className="mt-2 text-sm text-muted-foreground">{entity.description || "No description"}</p></div></div><div className="mt-6 space-y-3 border-t border-border/60 pt-4 text-sm"><div><span className="text-muted-foreground">Abilities: </span>{entity.abilities || "Not cataloged"}</div><div><span className="text-muted-foreground">Relationships: </span>{entity.relationships || "Not cataloged"}</div></div></CardContent></Card>)}</div></main></div>; }
