import { Link, useRoute } from "wouter";
import { ArrowLeft, GitCompareArrows, Star } from "lucide-react";
import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
export default function EntityPage() { const [, params] = useRoute("/entity/:id"); const id = Number(params?.id); const { data, isLoading } = trpc.catalog.entity.useQuery({ id }); if (isLoading) return <div className="grid min-h-screen place-items-center text-muted-foreground">Loading profile…</div>; if (!data) return <div className="container py-20"><Link href="/search">Back to search</Link><h1 className="mt-4 text-3xl font-bold">Profile not found</h1></div>; const { entity, appearances } = data; return <div className="min-h-screen bg-background"><header className="border-b border-border/60"><div className="container flex h-20 items-center justify-between"><Link href="/" className="font-display text-xl font-bold">Visual Works</Link><Link href="/compare" className="inline-flex items-center gap-2 text-sm text-primary"><GitCompareArrows size={17}/> Compare</Link></div></header><main className="container py-12"><Link href="/search" className="mb-8 inline-flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft size={16}/> Back to catalog</Link><div className="grid gap-10 lg:grid-cols-[280px_1fr]"><img src={entity.imageUrl || "https://images.unsplash.com/photo-1578632767115-351597cf2477?auto=format&fit=crop&w=700&q=80"} className="aspect-[4/5] w-full rounded-2xl object-cover" alt={entity.name}/><div><div className="eyebrow">{entity.kind} PROFILE</div><h1 className="section-title">{entity.name}</h1>{entity.nameAr && <p className="mt-2 text-xl text-muted-foreground">{entity.nameAr}</p>}<p className="mt-6 max-w-2xl leading-8 text-muted-foreground">{entity.description || "No description has been added yet."}</p><div className="mt-8 grid gap-4 md:grid-cols-2"><Card><CardContent className="p-5"><h2 className="font-display font-bold">Abilities</h2><p className="mt-3 text-sm leading-7 text-muted-foreground">{entity.abilities || "Not cataloged yet."}</p></CardContent></Card><Card><CardContent className="p-5"><h2 className="font-display font-bold">Relationships</h2><p className="mt-3 text-sm leading-7 text-muted-foreground">{entity.relationships || "Not cataloged yet."}</p></CardContent></Card></div></div></div><section className="mt-14"><div className="eyebrow">APPEARANCES</div><h2 className="section-title text-3xl">Across the archive</h2><div className="mt-5 grid gap-3 md:grid-cols-2">{appearances.map(({ work, role }) => <Link href={`/work/${work.id}`} key={work.id} className="rounded-xl border border-border/60 p-4 hover:border-primary"><div className="flex items-center justify-between"><span className="font-semibold">{work.title}</span><Star size={15} className="text-amber-400"/></div><div className="mt-1 text-xs text-muted-foreground">{role || "appears"} · {work.releaseYear || "—"}</div></Link>)}</div></section></main></div>; }
