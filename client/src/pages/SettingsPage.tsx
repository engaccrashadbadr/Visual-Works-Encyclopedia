import { Link } from "wouter";
import { ArrowLeft, Database, Globe2, Moon, Sun } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { useTheme } from "@/contexts/ThemeContext";
import { useLanguage } from "@/contexts/LanguageContext";
export default function SettingsPage() { const { theme, toggleTheme } = useTheme(); const { lang, toggleLang } = useLanguage(); const t = (en: string, ar: string) => lang === "ar" ? ar : en; return <div dir={lang === "ar" ? "rtl" : "ltr"} className="min-h-screen bg-background"><header className="border-b border-border/60"><div className="container flex h-20 items-center justify-between"><Link href="/" className="font-display text-xl font-bold">Visual Works <span className="text-primary">/</span> Settings</Link><Link href="/" className="inline-flex items-center gap-2 text-sm text-muted-foreground"><ArrowLeft size={16}/> {t("Home", "الرئيسية")}</Link></div></header><main className="container max-w-3xl py-12"><div className="eyebrow">PREFERENCES / الإعدادات</div><h1 className="section-title">{t("Make the archive yours", "خصص تجربة الأرشيف")}</h1><div className="mt-10 space-y-4"><Card><CardContent className="flex items-center justify-between gap-4 p-6"><div className="flex items-center gap-4"><Globe2 className="text-primary"/><div><h2 className="font-semibold">{t("Language", "اللغة")}</h2><p className="mt-1 text-sm text-muted-foreground">{t("Arabic and English with automatic RTL layout.", "العربية والإنكليزية مع اتجاه RTL تلقائي.")}</p></div></div><Button variant="outline" onClick={toggleLang}>{lang === "ar" ? "English" : "العربية"}</Button></CardContent></Card><Card><CardContent className="flex items-center justify-between gap-4 p-6"><div className="flex items-center gap-4">{theme === "dark" ? <Moon className="text-primary"/> : <Sun className="text-primary"/>}<div><h2 className="font-semibold">{t("Appearance", "المظهر")}</h2><p className="mt-1 text-sm text-muted-foreground">{t("Switch between a calm light mode and a focused dark mode.", "بدّل بين الوضع الفاتح والداكن.")}</p></div></div><Button variant="outline" onClick={toggleTheme}>{theme === "dark" ? t("Light mode", "الوضع الفاتح") : t("Dark mode", "الوضع الداكن")}</Button></CardContent></Card><Card className="border-primary/20 bg-primary/5"><CardContent className="flex items-center gap-4 p-6"><Database className="text-primary"/><div><h2 className="font-semibold">{t("Data sources", "مصادر البيانات")}</h2><p className="mt-1 text-sm leading-6 text-muted-foreground">AniList synchronization is available. TMDB remains optional until its API key is configured.</p></div></CardContent></Card></div></main></div>; }
